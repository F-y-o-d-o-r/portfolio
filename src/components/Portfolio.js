import React, { Component } from 'react';

class Portfolio extends Component {
  render() {
    return (
      <section className="portfolio">
        <div className="container">Portfolio Screen</div>
      </section>
    );
  }
}

export default Portfolio;
