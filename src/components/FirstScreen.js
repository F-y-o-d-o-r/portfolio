import React, { Component } from 'react';

class FirstScreen extends Component {
  componentDidMount() {}
  render() {
    return (
      <section className="first-screen">
        <div className="container">First Screen</div>
      </section>
    );
  }
}

export default FirstScreen;
