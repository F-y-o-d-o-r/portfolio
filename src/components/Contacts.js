import React, { Component } from 'react';

class Contacts extends Component {
  render() {
    return (
      <section className="contacts" id="contacts">
        <div className="container">Contacts Screen</div>
      </section>
    );
  }
}

export default Contacts;
